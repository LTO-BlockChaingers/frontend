import { NavbarTitleDirective } from './navbar-title.directive';

describe('NavbarTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarTitleDirective();
    expect(directive).toBeTruthy();
  });
});
